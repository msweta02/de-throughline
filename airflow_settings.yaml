airflow:
  connections:
  pools:
  variables:
    # Switch 1: the global kill switch. Throughline captures nothing without it,
    # and with it off the decorator is not even in the call path.
    - variable_name: throughline_enabled
      variable_value: "true"
